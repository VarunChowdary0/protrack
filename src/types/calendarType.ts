export interface CalendarType {
    id: string;
    participantId: string;
    projectId: string;
    date: string;
    title: string;
    description: string;
    createdAt: string;
    updatedAt: string;
}